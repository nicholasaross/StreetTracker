# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
.venv/
uv.lock.bak

# Build artifacts
dist/
build/

# Project artifacts (Nano-side, never commit)
*.engine
*.onnx
*.pt
*.weights
output/
camera_config.json
# StreetTracker convention: configs/camera.json carries the live Reolink
# password and is per-install. configs/camera.example.json IS tracked.
configs/camera.json
*.mp4
*.mkv

# Editor noise
.vscode/
.idea/
*.swp
.DS_Store

# Claude Code workspace: per-install operator artifacts, scratch
# scripts, sample-snap bundles, embedded worktrees. Never committed --
# all checked-in CLAUDE-related content goes in CLAUDE.md and src/.
.claude/
